# Local Development Environment Variables
# Copy this file to .env.local and fill in your sandbox/test credentials
# This file is gitignored - your actual credentials won't be committed

# Authorize.net Sandbox/Test Credentials (used for local dev, Vercel previews)
# Get these from: https://developer.authorize.net/hello_world/sandbox/
AUTHORIZE_NET_LOGIN_ID_SANDBOX=your_sandbox_login_id_here
AUTHORIZE_NET_TRANSACTION_KEY_SANDBOX=your_sandbox_transaction_key_here

# (Optional) Legacy fallback for local-only scripts that still read unsuffixed vars.
# Avoid setting these in Vercel to prevent sandbox/prod collisions.
# AUTHORIZE_NET_LOGIN_ID=your_sandbox_login_id_here
# AUTHORIZE_NET_TRANSACTION_KEY=your_sandbox_transaction_key_here

# Optional: Force environment (leave commented out unless needed)
# Valid values: sandbox, production
# AUTHORIZE_NET_ENVIRONMENT=sandbox

# Optional: Override how Vercel preview deployments behave
# (defaults to sandbox unless explicitly set)
# AUTHORIZE_NET_PREVIEW_ENVIRONMENT=sandbox

# Production credentials should ONLY be defined inside Vercel (Production scope)
# AUTHORIZE_NET_LOGIN_ID_PROD=your_production_login_id
# AUTHORIZE_NET_TRANSACTION_KEY_PROD=your_production_transaction_key

# GoHighLevel API Credentials and Configuration
# Obtain values from https://app.gohighlevel.com/ under Settings → Company → API Keys
GHL_API_KEY=your_gohighlevel_api_key_here

# Optional: override the REST API base URL (defaults to https://services.leadconnectorhq.com/)
# GHL_API_BASE_URL=https://services.leadconnectorhq.com/
# Optional: override the GoHighLevel API version header (defaults to 2021-07-28)
# GHL_API_VERSION=2021-07-28

# Required identifiers for contact management
GHL_LOCATION_ID=your_gohighlevel_location_id

# Pipeline automation (optional if managed via workflows)
# GHL_PIPELINE_ID=your_pipeline_id
# GHL_WON_STAGE_ID=your_pipeline_won_stage_id

# Custom field IDs for tracking last purchase metadata (optional but recommended)
GHL_FIELD_LAST_PURCHASE_DATE=custom_field_id_for_last_purchase_date
GHL_FIELD_LAST_PRODUCT=custom_field_id_for_last_product
GHL_FIELD_LAST_PURCHASE_AMOUNT=custom_field_id_for_last_purchase_amount

# Optional tag prefix applied to product-based tags
# GHL_PRODUCT_TAG_PREFIX=customer-product-

